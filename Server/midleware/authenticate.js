const jwt = require('jsonwebtoken');

function authenticateUser(req,res,next) {
    // let token = req.body.token;
    // let tokenDetails = jwt.verify(token,JWT_SECRET_KEY);
    // // we can set token at the time of user login.
    // // let token = jwt.sign({},JWT_SECRET_KEY) // expiration time is optinal
    // // we can just verify the user details from database. we recieve details tokendetails.

    // if(toekendtails === userDetails){
    //     next();
    // }

}